Install 10 packages (user linadmin, pass 11): make patch flex bison gv inkscape ed gawk libc6-dev python3-pyqt5
== fly-term ==
1) In launcher add --workdir /tmp --hide-tabbar
2) Disable scrollbar
3) Maximize window
4) Set font 'Liberation Mono 24' ('stty size' must show '25 100')
5) Set color scheme 'black on white'
6) Add '\nopagenumbers' to command menu
7) Set unlimited scrollback history
8) cp /usr/share/konsole/default.keytab ~/.local/share/konsole/
     and change x7f -> b
===============================================================
cat ~/lhplain/bashrc >>~/.bashrc
mkdir ~/bin
Re-login
== mytex ==
Unpack mytex repo
ln -sf open.okular open
ln -sf printer.okular printer
for i in make-texmf tex tex+ tex- mf dvi+ dvi- ctangle cweave tie ctie; do
  ln -s ~/mytex/$i ~/bin/
done
== cweb ==
Unpack cweb repo
make
== em ==
Unpack em repo
Create ~/bin/em from em/README
== tex ==
Unpack tex repo
Adjust path.ch
make
== mf ==
Unpack mf repo
Adjust path.ch
echo 200x100 >~/.gv-size # adjust empirically
echo 400x300 >~/.screen-size # adjust empirically
make
== dvitype ==
Unpack dvitype repo
Adjust path.ch
make
Create ~/bin/dvitype from dvitype/README and
  change /path/to -> ~
== gftodvi ==
Unpack gftodvi repo
Adjust path.ch
make
Create ~/bin/gftodvi from gftodvi/README and
  change /path/to -> ~
== gftopk ==
Unpack gftopk repo
make
Create ~/bin/gftopk from gftopk/README and
  change /path/to -> ~
== okular ==
Disable 'Показывать заголовок документа в заголовке окна, если возможно'
(non-ASCII characters in tex filenames are displayed incorrectly)
== dvips ==
Unpack dvips repo (differs from dvips/ in http://ftp.cs.stanford.edu/pub/dvips/dvips576.tar.gz)
make
cp ~/th/gbsnu/* ~/dvips/
cp ~/th/gbsnu.map ~/dvips/
echo p +gbsnu.map >~/.dvipsrc
ln -s ~/th/chinese ~/bin/
== keyboard ==
15 characters are already missing in /usr/share/X11/xkb/symbols/ru -- `[]'~@#$^&{}|<>
Just make another two characters missing via replacing \/ with «» by changing BKSL line
  in /usr/share/X11/xkb/symbols/ru to use guillemotleft, guillemotright
Make Menu the language switching key
